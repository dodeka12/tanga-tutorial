"""TanGA Tutorial — metadata-only package.

This package exists so hatch-vcs can provide dynamic versioning (via
``importlib.metadata.version("tanga-tutorial")``). The actual content lives in
the ``tutorials/`` directory.
"""
